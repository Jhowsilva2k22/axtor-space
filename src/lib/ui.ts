// Classes compartilhadas de UI — fonte única pra estilos repetidos.

// Label de campo das telas externas (login, signup, recuperar/reset senha, checkout).
// Peso `font-medium`: legível e confortável, sem o peso do negrito.
// Mudou aqui, mudou em todas as telas que usam.
export const fieldLabel =
  "block text-[10px] font-medium uppercase tracking-[0.2em] text-muted-foreground/80";
