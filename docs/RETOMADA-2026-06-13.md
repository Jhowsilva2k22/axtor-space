# RETOMADA — Axtor Space (pra começar o próximo chat)

> Cola isto (ou abre) no começo do próximo chat. Resumo completo de onde paramos
> na madrugada de 2026-06-12. Tudo abaixo já está NO AR salvo o que estiver marcado ⚠️/EM ABERTO.

## Contexto rápido

- **Produto:** Axtor Space — SaaS multi-tenant (link na bio + funil + diagnóstico imersivo). Voz SaaS neutra. Separado de Habithus e Pai Presente.
- **Repo:** github.com/Jhowsilva2k22/axtor-space · branch `main`.
- **Deploy:** **Vercel** a partir do `main` (merge na main → publica sozinho). **NÃO existe Lovable** no projeto (a instrução do projeto que cita Lovable está errada — ignorar).
- **Banco PROD (real):** Supabase ref `pybgqassjzcynzaakzhz` (nome "axtor-staging", mas é produção). O `bdxkcfngskagriaapepo` é ANTIGO, não usar.
- **Stack:** React 18 + TS + Vite + Tailwind + shadcn/ui + Supabase. Mobile-first. RLS sempre. Sem emoji em UI.
- **Git só roda no Windows do Joanderson** (o git do sandbox do agente corrompe o index). Fluxo: edita local → testa em aba anônima → ok → branch + PR → merge → Vercel.
- **Antes de codar:** `git pull` na main e ler `MEMORY.md` (raiz) + `docs/CHECKPOINT-2026-06-12.md`.

## O que entrou no ar em 2026-06-12 (#173 e #174)

- **Guest checkout Pix** ("Caminho Y": paga primeiro, conta depois) — NO AR, validado com Pix real de R$6.
- **Fix de domínio** — links canônicos em `axtor.space` (PUBLIC_BASE_URL).
- **Painel mobile-first** — bottom nav, app bar compacta, bordas padrão (`rounded-xl/2xl`, `border-gold/20`), fix do plano **Premium** (não bloqueia mais recursos pagos), date picker custom que funciona no iPhone, rotas por seção (`/painel/secao/:section`).
- **Fix do link do diagnóstico** — a raiz `/` virou a página de vendas; o diagnóstico vive em `/diagnostico`. Os botões "Diagnóstico de Instagram" das bios estavam indo pra `/` (caíam na venda). Corrigido no banco (todos os tenants) e no gerador do painel: agora `/diagnostico?utm_source=<slug>`.
- **Perf** — fundo 3D (Three.js) carrega depois da 1ª pintura (lazy).
- **Jargão** — "tenant" e termos técnicos fora dos textos visíveis.

## ⚠️ PRIORIDADE ABERTA — Desempenho mobile de /vendas e /planos

- PageSpeed **mobile ~60** (desktop 93-96, ótimo). O lazy do fundo 3D **não resolveu**.
- **Causa:** são um app React **client-side** — o HTML chega vazio e o navegador precisa baixar+rodar todo o JS antes de pintar (FCP/LCP ~6s no 4G). O fundo 3D era só parte.
- **Solução real (a fazer):** **pré-renderizar** `/vendas` e `/planos` (SSG/prerender no build da Vercel, ex.: `vite-plugin-ssr`/prerender ou react-snap). Serve HTML pronto → FCP/LCP despencam → faixa de 85-95. Mudança de build, **não destrutiva**. Testar local e medir de verdade — sem prometer número antes de medir.

## Outras pendências (menor prioridade)

- **Mockup do celular na /vendas** (`PhoneMockup3`) ainda é PROVISÓRIO. Joanderson vai mandar **3 prints reais** (proporção 9:19, só o conteúdo da tela, sem barra do navegador): (1) bio, (2) pergunta do diagnóstico, (3) resultado/direcionamento. Trocar quando chegarem.
- **CRLF:** o Windows mostra arquivos como "modified" por fim de linha. Travou o merge do #174 (resolvido com `git stash`). Considerar `.gitattributes` com `* text=auto eol=lf` pra acabar com o atrito.
- **Fase 6 / Pix:** caminho pagamento→ativação→/bem-vindo pronto; o guest checkout já validou um Pix real. QA ponta a ponta amplo segue como higiene.
- **M2:** ativar "leaked password protection" no Supabase Auth (ação do dono).
- **OG por tenant:** código no ar; falta só testar num WhatsApp real + "Scrape Again" no depurador do FB.
- **Segurança (não urgente):** o que vazou no histórico do repo público é só a anon key (pública por design, protegida por RLS). Rotação = higiene opcional, não emergência. Repo privado/purgar histórico fica a critério.

## Como o link de captura funciona (pergunta que ficou em aberto)

- Link direto do diagnóstico do Joanderson: `axtor.space/diagnostico?utm_source=joanderson&utm_medium=instagram`. O `utm_source` resolve o tenant E atribui o lead.
- Onde colar: story (figurinha de link), WhatsApp, CTA de post, anúncio, QR. Na bio do Instagram, usar a página `axtor.space/joanderson` (hub completo).
- Leads aparecem no Painel → aba Leads (volume em Métricas).
- Pra link curto/bonito: usar campanha (`axtor.space/r/<slug>` redireciona pro destino com utm) — `RedirectCampaign` já suporta rota interna `/diagnostico`.

## Regras de ouro (lembrete)

1. Cada edit resolve UM problema, sem refatorar de carona.
2. Mudança >2 linhas ou arquivo crítico: mostrar diff antes.
3. Não commitar/pushar sem ok. Nunca push direto na main — branch + PR.
4. Banco/produção/deploy: mostrar plano, esperar "ok".
5. Sem moralismo, sem clichê de coach, sem emoji em UI.
6. Ao concluir algo importante: atualizar MEMORY.md/CHECKPOINT/mem no mesmo fluxo.
